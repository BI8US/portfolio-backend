POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

DATABASE_URL=

PORT=8080

JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=86400

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

OPENAI_API_KEY=
# Optional: base URL for OpenAI-compatible servers. Default path is /v1/chat/completions — OpenAI’s
# “chat completions” API (messages in, assistant reply out). Same contract as api.openai.com.
# OPENAI_API_URL=https://api.openai.com/v1/chat/completions
# OPENAI_MODEL=gpt-4o-mini
